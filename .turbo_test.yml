install: |
  sudo apt-get update -y
  sudo apt-get install make upstart gcc g++ make -y --no-install-recommends

setup: |
  echo 'SETUP'

test_suite:
  command: sh

  files:
    - test/*_test.sh

  env:
    - FOO=bar